{{#if singlePlaylist.songs}}

  <table class="ui fixed striped table">
    <thead>
      <tr>
        <th>Song</th>
        <th>Artist</th>
        <th>Genre</th>
        <th>Duration</th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      {{#each singlePlaylist.songs}}
        <tr>
          <td>
            {{title}}
          </td>
          <td>
            {{artist}}
          </td>
          <td>
            {{genre}}
          </td>
          <td>
            {{duration}}
          </td>
          <td class="center aligned">
            <a href="/playlist/{{../singlePlaylist.id}}/deletesong/{{id}}" class="ui icon button" title="Delete song">
              <i class="ui trash icon"></i>
            </a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>

{{else}}
  <h3 class="ui message">
    Empty playlist!
  </h3>

{{/if}}